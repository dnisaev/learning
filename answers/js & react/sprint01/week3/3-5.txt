const user = {}
const address = user
const student = {}


if(address === user) {
    console.log ("Yo!!!")
}

/*Какую переменную надо указать вместо XXX, кроме user, что бы в консоль вывелась строка "Yo!!!?*/ address